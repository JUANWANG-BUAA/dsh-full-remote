# Insert the provider-neutral reverse proxy into the Web composition.
# Install with: dsh plugin --profile web add ./dsh-remote
- insert:
    - id: reverse-proxy
      name: dsh-reverse-proxy
      config:
        listenHost: 127.0.0.1
        listenPort: 3081
